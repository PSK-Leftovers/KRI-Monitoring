name: KRI-Monitoring

services:
  database:
    image: postgres:17
    environment:
      POSTGRES_DB: kri
      POSTGRES_USER: kri
      POSTGRES_PASSWORD: kri
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: